apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: istio
spec:
  interval: 30m
  chart:
    spec:
      chart: istio
      version: "1.24.0"
      sourceRef:
        kind: HelmRepository
        name: istio
      interval: 12h
  install:
    crds: Create
  upgrade:
    crds: CreateReplace