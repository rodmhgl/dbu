"""{{WORKLOAD_NAME}} — FastAPI web application."""

from fastapi import FastAPI

app = FastAPI(title="{{WORKLOAD_NAME}}", version="0.1.0")


@app.get("/health")
async def health():
    return {"status": "healthy"}


@app.get("/")
async def root():
    return {"message": "{{WORKLOAD_NAME}} is running"}


if __name__ == "__main__":
    import uvicorn

    uvicorn.run(app, host="0.0.0.0", port={{PORT}})
