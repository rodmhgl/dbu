# {{WORKLOAD_NAME}}

Long-running worker process scaffolded by the Teams API.

## Development

```bash
pip install -r requirements.txt
python -m src.main
```

## Docker

```bash
docker build -t {{WORKLOAD_NAME}} .
docker run {{WORKLOAD_NAME}}
```
